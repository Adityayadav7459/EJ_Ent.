# --- MINIO STORAGE SETUP ---
MINIO_ROOT_USER=local_dev_user
MINIO_ROOT_PASSWORD=local_dev_password

# --- FASTAPI BACKEND ---
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_ACCESS_KEY=local_dev_user
STORAGE_SECRET_KEY=local_dev_password
STORAGE_BUCKET_NAME=ej-ent-videos

# --- DATABASE & BROKER ---
DATABASE_URL=postgresql://user:password@localhost:5432/datavault
REDIS_URL=redis://localhost:6379

# --- OAUTH ---
# (Contributors must generate their own Google Cloud Console credentials)