server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # The Magic SPA Rule: Try the file, then try the directory, then fallback to React
    location / {
        try_files $uri $uri/ /index.html;
    }
}